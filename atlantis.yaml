version: 3
projects:
- name: deploy-aws
  dir: .
  autoplan:
    enabled: true
  workflow: deploy

workflows:
  deploy:
    plan:
      steps:
      - init
      - plan
    apply:
      steps:
      - apply